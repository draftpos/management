<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.management</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="90"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Management App" string="Management App" name="management_app" groups="base.group_system">
                    <block title="Marketing Configuration" name="marketing_config">
                        <setting id="management_expected_leads" help="Set the default expected leads for new marketing campaigns.">
                            <field name="management_default_expected_leads"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="management_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module' : 'management_app', 'bin_size': False}</field>
    </record>

    <menuitem id="management_menu_configuration_settings"
        name="Settings"
        parent="management_menu_configuration"
        sequence="0"
        action="management_config_settings_action"
        groups="base.group_system"/>
</odoo>
